已经把 3.14 版本前端和后端基础接口联调通了。

目前我改了这些：
1.把原来的模拟上传改成真实上传
2.api/index.js 已经接好了真实接口
3.MainPage.js 里的按钮和上传逻辑也改好了

现在已经能正常跑：
/upload
/tasks/{task_id}

现在日志里能看到：
POST /upload 200 OK
GET /tasks/{task_id} 200 OK

GET /fields/{task_id} 现在返回 404，不是接口错了，是因为前端流程里还没继续调：
/parse/{task_id}
/extract/{task_id}

所以你接下来就在我现在这版代码基础上，把这两个步骤补上，再去查 /fields/{task_id} 就行。

完整流程：
upload
→ tasks
→ parse
→ extract
→ fields

前端修改说明：
1.修改了接口请求文件
frontend 3.14/project/src/api/index.js：
将原来的 mock 上传逻辑改为真实上传逻辑
接入后端 POST /upload
新增任务查询接口封装 GET /tasks/{task_id}
新增字段查询接口封装 GET /fields/{task_id}
后续可继续在此基础上补 POST /parse/{task_id} 和 POST /extract/{task_id}

2.修改了主页面逻辑
frontend 3.14/project/src/pages/MainPage.js：
将“模拟上传”按钮改为“上传文件”
上传按钮点击后，调用真实 uploadFiles
上传成功后读取返回的 task_id
自动请求 GET /tasks/{task_id}
自动请求 GET /fields/{task_id}
根据返回结果更新页面状态信息

